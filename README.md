# Instructions to run Project 2:
```
cd Project 2

# On Windows:
python -m venv venv
venv\Scripts\activate

# On macOS/Linux:
python3 -m venv venv
source venv/bin/activate
  
pip install -r requirements.txt
streamlit run project.py
deactivate (after finish working, to deactivate virtual environment)
```
